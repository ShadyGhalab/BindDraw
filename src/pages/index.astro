---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="Invisible Ink — Draw in the dark. Reveal the magic.">

  <!-- ===== HERO SECTION ===== -->
  <section class="hero">
    <div class="hero__bg" aria-hidden="true">
      <div class="glow-orb glow-orb--purple" style="top: -200px; left: -100px;"></div>
      <div class="glow-orb glow-orb--pink" style="top: 100px; right: -150px;"></div>
      <div class="glow-orb glow-orb--blue" style="bottom: -100px; left: 30%;"></div>
    </div>
    <div class="container hero__inner">
      <div class="hero__content">
        <div class="hero__badge reveal">
          <span class="hero__badge-dot"></span>
          Available on the App Store
        </div>
        <h1 class="hero__title reveal" style="transition-delay: 0.1s;">
          Draw.<br />
          <span class="gradient-text">Glow.</span><br />
          Reveal.
        </h1>
        <p class="hero__subtitle reveal" style="transition-delay: 0.2s;">
          Create art in complete darkness, then watch it come alive with stunning reveal effects. 
          Seven magical styles. Endless creativity. Pure wonder.
        </p>
        <div class="hero__actions reveal" style="transition-delay: 0.3s;">
          <a href="https://apps.apple.com/app/id6758377109" class="app-store-badge" target="_blank" rel="noopener">
            <img src="https://tools.applemediaservices.com/api/badges/download-on-the-app-store/white/en-us?size=250x83" alt="Download on the App Store" />
          </a>
          <a href={`${base}features/`} class="btn btn--secondary">
            Explore Features
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M6 3l5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </a>
        </div>
      </div>
      <div class="hero__visual reveal--glow" style="transition-delay: 0.3s;">
        <div class="hero__phone-glow" aria-hidden="true"></div>
        <img src={`${base}images/hero.png`} alt="Invisible Ink app - Draw. Glow. Reveal." class="hero__screenshot" width="400" height="867" />
      </div>
    </div>
    <div class="hero__scroll-hint" aria-hidden="true">
      <div class="hero__scroll-line"></div>
    </div>
  </section>

  <!-- ===== HOW IT WORKS ===== -->
  <section class="section how-it-works">
    <div class="container">
      <div class="text-center reveal">
        <span class="section-label">How It Works</span>
        <h2>Magic in three steps</h2>
        <p style="max-width: 540px; margin: 1rem auto 0;">No skills required. Just draw, reveal, and be amazed.</p>
      </div>
      <div class="steps reveal-stagger">
        <div class="step">
          <div class="step__number">1</div>
          <div class="step__icon">
            <svg width="40" height="40" viewBox="0 0 40 40" fill="none"><circle cx="20" cy="20" r="18" stroke="url(#g1)" stroke-width="2"/><path d="M14 26l4-16 4 10 4-6" stroke="url(#g1)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><defs><linearGradient id="g1" x1="0" y1="0" x2="40" y2="40"><stop stop-color="#ff6b9d"/><stop offset="1" stop-color="#a855f7"/></linearGradient></defs></svg>
          </div>
          <h3>Draw in the Dark</h3>
          <p>Your strokes are invisible. Draw freely on the black canvas — trust your instincts.</p>
        </div>
        <div class="step__connector" aria-hidden="true">
          <svg width="60" height="2" viewBox="0 0 60 2"><line x1="0" y1="1" x2="60" y2="1" stroke="url(#gc)" stroke-width="2" stroke-dasharray="6 4"/><defs><linearGradient id="gc"><stop stop-color="#a855f7" stop-opacity="0.3"/><stop offset="1" stop-color="#a855f7"/></linearGradient></defs></svg>
        </div>
        <div class="step">
          <div class="step__number">2</div>
          <div class="step__icon">
            <svg width="40" height="40" viewBox="0 0 40 40" fill="none"><circle cx="20" cy="20" r="6" fill="url(#g2)" opacity="0.3"/><circle cx="20" cy="20" r="12" stroke="url(#g2)" stroke-width="2" opacity="0.5"/><circle cx="20" cy="20" r="18" stroke="url(#g2)" stroke-width="2"/><circle cx="20" cy="20" r="3" fill="url(#g2)"/><defs><linearGradient id="g2" x1="0" y1="0" x2="40" y2="40"><stop stop-color="#a855f7"/><stop offset="1" stop-color="#3b82f6"/></linearGradient></defs></svg>
          </div>
          <h3>Choose a Reveal Style</h3>
          <p>Pick from 7 magical effects — Firefly, Lightning, Frost, Glitter, and more.</p>
        </div>
        <div class="step__connector" aria-hidden="true">
          <svg width="60" height="2" viewBox="0 0 60 2"><line x1="0" y1="1" x2="60" y2="1" stroke="url(#gc2)" stroke-width="2" stroke-dasharray="6 4"/><defs><linearGradient id="gc2"><stop stop-color="#3b82f6" stop-opacity="0.3"/><stop offset="1" stop-color="#22d3ee"/></linearGradient></defs></svg>
        </div>
        <div class="step">
          <div class="step__number">3</div>
          <div class="step__icon">
            <svg width="40" height="40" viewBox="0 0 40 40" fill="none"><path d="M20 4l4.5 9.1 10 1.5-7.2 7 1.7 10L20 27l-9 4.6 1.7-10-7.2-7 10-1.5z" stroke="url(#g3)" stroke-width="2" stroke-linejoin="round"/><defs><linearGradient id="g3" x1="0" y1="0" x2="40" y2="40"><stop stop-color="#22d3ee"/><stop offset="1" stop-color="#34d399"/></linearGradient></defs></svg>
          </div>
          <h3>Share the Magic</h3>
          <p>Save to your gallery, share with friends, or challenge them to a game.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== SHOWCASE (Screenshots) ===== -->
  <section class="section showcase">
    <div class="showcase__bg" aria-hidden="true">
      <div class="glow-orb glow-orb--purple" style="top: 50%; left: 50%; transform: translate(-50%, -50%);"></div>
    </div>
    <div class="container">
      <div class="showcase__row">
        <div class="showcase__text reveal--left">
          <span class="section-label">Free Draw</span>
          <h2>Bring Art to Life</h2>
          <p>Create your masterpiece on a dark canvas with a full palette of colors, brushes, and tools. Every stroke is invisible until you choose to reveal it.</p>
          <ul class="feature-list">
            <li>
              <span class="feature-list__dot" style="background: var(--accent-pink);"></span>
              Full color palette with custom colors
            </li>
            <li>
              <span class="feature-list__dot" style="background: var(--accent-purple);"></span>
              8 premium brush packs
            </li>
            <li>
              <span class="feature-list__dot" style="background: var(--accent-blue);"></span>
              7 stunning reveal effects
            </li>
          </ul>
        </div>
        <div class="showcase__image reveal--right">
          <img src={`${base}images/free-draw.png`} alt="Bring Art to Life — Free Draw mode" class="screenshot" />
        </div>
      </div>

      <div class="showcase__row showcase__row--reverse">
        <div class="showcase__text reveal--right">
          <span class="section-label">Reveal Styles</span>
          <h2>Reveal the Magic</h2>
          <p>Choose from 7 enchanting reveal styles. Watch frost melt away, lightning flash, fireflies trace your lines, or glitter settle into shape.</p>
          <div class="reveal-styles-grid">
            <span class="reveal-style-tag">Darkroom</span>
            <span class="reveal-style-tag">Firefly</span>
            <span class="reveal-style-tag reveal-style-tag--accent">Frost</span>
            <span class="reveal-style-tag reveal-style-tag--accent">Lightning</span>
            <span class="reveal-style-tag reveal-style-tag--accent">Grow</span>
            <span class="reveal-style-tag reveal-style-tag--accent">Glitter</span>
            <span class="reveal-style-tag reveal-style-tag--accent">Watercolor</span>
          </div>
        </div>
        <div class="showcase__image reveal--left">
          <img src={`${base}images/reveal.png`} alt="Reveal the Magic — 7 reveal styles" class="screenshot" />
        </div>
      </div>
    </div>
  </section>

  <!-- ===== GAME MODES ===== -->
  <section class="section games">
    <div class="container">
      <div class="text-center reveal">
        <span class="section-label">Party Games</span>
        <h2>More than just drawing</h2>
        <p style="max-width: 560px; margin: 1rem auto 0;">Challenge friends, test your memory, race the clock — or just vibe with your daily creative challenge.</p>
      </div>
      <div class="games__grid reveal-stagger">
        <a href={`${base}features/`} class="game-card game-card--guess">
          <div class="game-card__visual">
            <img src={`${base}images/guess-game.png`} alt="Guess What I Drew" />
          </div>
          <div class="game-card__content">
            <h3>Guess What I Drew</h3>
            <p>Pass-and-play multiplayer. One draws blind, others guess. Perfect for family game night.</p>
            <span class="game-card__cta">Learn more &rarr;</span>
          </div>
        </a>
        <a href={`${base}features/`} class="game-card game-card--copycat">
          <div class="game-card__visual">
            <img src={`${base}images/copycat.png`} alt="Copycat Challenge" />
          </div>
          <div class="game-card__content">
            <h3>Copycat Challenge</h3>
            <p>Memorize an image, draw it from memory, and score your accuracy. Can you hit Diamond?</p>
            <span class="game-card__cta">Learn more &rarr;</span>
          </div>
        </a>
        <a href={`${base}features/`} class="game-card game-card--speed">
          <div class="game-card__content game-card__content--full">
            <div class="game-card__icon">
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none"><circle cx="24" cy="24" r="20" stroke="url(#gs)" stroke-width="2"/><path d="M24 14v10l7 7" stroke="url(#gs)" stroke-width="2.5" stroke-linecap="round"/><defs><linearGradient id="gs" x1="4" y1="4" x2="44" y2="44"><stop stop-color="#fbbf24"/><stop offset="1" stop-color="#ff6b9d"/></linearGradient></defs></svg>
            </div>
            <h3>Speed Draw</h3>
            <p>Race against the clock. Draw as fast as you can before time runs out!</p>
            <span class="game-card__cta">Learn more &rarr;</span>
          </div>
        </a>
        <a href={`${base}features/`} class="game-card game-card--daily">
          <div class="game-card__content game-card__content--full">
            <div class="game-card__icon">
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none"><rect x="6" y="10" width="36" height="32" rx="4" stroke="url(#gd)" stroke-width="2"/><path d="M6 20h36M16 6v8M32 6v8" stroke="url(#gd)" stroke-width="2" stroke-linecap="round"/><circle cx="24" cy="31" r="4" fill="url(#gd)"/><defs><linearGradient id="gd" x1="6" y1="6" x2="42" y2="42"><stop stop-color="#34d399"/><stop offset="1" stop-color="#3b82f6"/></linearGradient></defs></svg>
            </div>
            <h3>Daily Challenge</h3>
            <p>A new creative prompt every day. Build your streak and never miss a beat.</p>
            <span class="game-card__cta">Learn more &rarr;</span>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- ===== PREMIUM TEASER ===== -->
  <section class="section premium">
    <div class="premium__bg" aria-hidden="true">
      <div class="glow-orb glow-orb--pink" style="top: 50%; left: 20%; transform: translate(-50%, -50%); opacity: 0.1;"></div>
      <div class="glow-orb glow-orb--purple" style="top: 50%; right: 10%; transform: translate(50%, -50%); opacity: 0.1;"></div>
    </div>
    <div class="container">
      <div class="premium__card reveal--glow">
        <div class="premium__content">
          <span class="section-label">Premium</span>
          <h2>Unlock Your Full Potential</h2>
          <p>One purchase. Everything unlocked. No subscriptions.</p>
          <div class="premium__features">
            <div class="premium__feature">
              <span class="premium__check">&#10003;</span>
              All 7 reveal styles
            </div>
            <div class="premium__feature">
              <span class="premium__check">&#10003;</span>
              8 professional brush packs
            </div>
            <div class="premium__feature">
              <span class="premium__check">&#10003;</span>
              48 canvas themes
            </div>
            <div class="premium__feature">
              <span class="premium__check">&#10003;</span>
              300+ drawing prompts
            </div>
            <div class="premium__feature">
              <span class="premium__check">&#10003;</span>
              Unlimited gallery
            </div>
            <div class="premium__feature">
              <span class="premium__check">&#10003;</span>
              Ad-free experience
            </div>
            <div class="premium__feature">
              <span class="premium__check">&#10003;</span>
              iCloud sync
            </div>
            <div class="premium__feature">
              <span class="premium__check">&#10003;</span>
              AI drawing coach
            </div>
          </div>
          <div class="premium__price">
            <span class="premium__amount">$3.99</span>
            <span class="premium__label">One-time purchase</span>
          </div>
          <a href="https://apps.apple.com/app/id6758377109" class="btn btn--primary" target="_blank" rel="noopener">
            Get Premium
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== FINAL CTA ===== -->
  <section class="section cta">
    <div class="container text-center reveal--glow">
      <h2>Ready to draw in the dark?</h2>
      <p style="max-width: 480px; margin: 1rem auto 2rem;">Download Invisible Ink and discover the magic of invisible drawing.</p>
      <a href="https://apps.apple.com/app/id6758377109" class="app-store-badge" target="_blank" rel="noopener">
        <img src="https://tools.applemediaservices.com/api/badges/download-on-the-app-store/white/en-us?size=250x83" alt="Download on the App Store" />
      </a>
    </div>
  </section>

</BaseLayout>

<style>
  /* ===== HERO ===== */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    overflow: hidden;
    padding-top: 5rem;
  }

  .hero__bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
  }

  .hero__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 4rem;
    position: relative;
    z-index: 1;
  }

  .hero__badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    background: rgba(168, 85, 247, 0.08);
    border: 1px solid rgba(168, 85, 247, 0.15);
    border-radius: 100px;
    font-size: 0.85rem;
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
    width: fit-content;
  }

  .hero__badge-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--accent-green);
    box-shadow: 0 0 8px var(--accent-green);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .hero__title {
    font-size: clamp(3rem, 7vw, 5.5rem);
    font-weight: 900;
    line-height: 1.05;
    letter-spacing: -0.03em;
    margin-bottom: 1.5rem;
  }

  .hero__subtitle {
    font-size: clamp(1.05rem, 1.8vw, 1.25rem);
    line-height: 1.7;
    max-width: 480px;
    margin-bottom: 2rem;
  }

  .hero__actions {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero__visual {
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
  }

  .hero__phone-glow {
    position: absolute;
    width: 300px;
    height: 500px;
    background: radial-gradient(ellipse, var(--glow-purple), transparent 70%);
    filter: blur(60px);
    z-index: 0;
  }

  .hero__screenshot {
    position: relative;
    z-index: 1;
    max-width: 340px;
    width: 100%;
    height: auto;
    border-radius: var(--radius-xl);
    animation: heroFloat 6s ease-in-out infinite;
  }

  @keyframes heroFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-12px); }
  }

  .hero__scroll-hint {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
  }

  .hero__scroll-line {
    width: 1px;
    height: 60px;
    background: linear-gradient(to bottom, var(--accent-purple), transparent);
    animation: scrollHint 2s ease-in-out infinite;
  }

  @keyframes scrollHint {
    0% { opacity: 0; transform: scaleY(0); transform-origin: top; }
    50% { opacity: 1; transform: scaleY(1); transform-origin: top; }
    100% { opacity: 0; transform: scaleY(1); transform-origin: bottom; }
  }

  /* ===== HOW IT WORKS ===== */
  .text-center {
    text-align: center;
  }

  .steps {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: 1rem;
    margin-top: 4rem;
  }

  .step {
    text-align: center;
    max-width: 260px;
    position: relative;
  }

  .step__number {
    position: absolute;
    top: -10px;
    right: -10px;
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--bg-card);
    border: var(--border-glow);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--accent-purple);
  }

  .step__icon {
    width: 80px;
    height: 80px;
    margin: 0 auto 1.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg-card);
    border: var(--border-subtle);
    border-radius: 50%;
  }

  .step h3 {
    margin-bottom: 0.5rem;
  }

  .step p {
    font-size: 0.95rem;
  }

  .step__connector {
    margin-top: 40px;
    flex-shrink: 0;
  }

  /* ===== SHOWCASE ===== */
  .showcase {
    position: relative;
    overflow: hidden;
  }

  .showcase__bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .showcase__row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    margin-bottom: 6rem;
    position: relative;
    z-index: 1;
  }

  .showcase__row--reverse {
    direction: rtl;
  }

  .showcase__row--reverse > * {
    direction: ltr;
  }

  .showcase__text h2 {
    margin-bottom: 1rem;
  }

  .showcase__text p {
    margin-bottom: 1.5rem;
    line-height: 1.8;
  }

  .showcase__image {
    display: flex;
    justify-content: center;
  }

  .showcase__image img {
    max-width: 320px;
    width: 100%;
    border-radius: var(--radius-xl);
  }

  .feature-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .feature-list li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: var(--text-secondary);
    font-size: 0.95rem;
  }

  .feature-list__dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .reveal-styles-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .reveal-style-tag {
    padding: 0.4rem 0.9rem;
    border-radius: 100px;
    font-size: 0.85rem;
    font-weight: 500;
    background: var(--bg-card);
    border: var(--border-subtle);
    color: var(--text-secondary);
  }

  .reveal-style-tag--accent {
    border-color: rgba(168, 85, 247, 0.2);
    color: var(--accent-purple);
    background: rgba(168, 85, 247, 0.08);
  }

  /* ===== GAME MODES ===== */
  .games__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .game-card {
    background: var(--gradient-card);
    border: var(--border-subtle);
    border-radius: var(--radius-lg);
    overflow: hidden;
    transition: all 0.4s var(--ease-out-expo);
    display: flex;
    flex-direction: column;
    text-decoration: none;
    cursor: pointer;
  }

  .game-card:hover {
    border-color: rgba(168, 85, 247, 0.2);
    transform: translateY(-4px);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }

  .game-card__visual {
    padding: 1.5rem 1.5rem 0;
    display: flex;
    justify-content: center;
  }

  .game-card__visual img {
    max-width: 260px;
    width: 100%;
    border-radius: var(--radius-md);
  }

  .game-card__content {
    padding: 1.5rem;
  }

  .game-card__content--full {
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    height: 100%;
  }

  .game-card__icon {
    margin-bottom: 1.25rem;
  }

  .game-card h3 {
    margin-bottom: 0.5rem;
    font-size: 1.25rem;
  }

  .game-card p {
    font-size: 0.95rem;
    margin-bottom: 1rem;
  }

  .game-card__cta {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--accent-purple);
    transition: color 0.3s;
  }

  .game-card:hover .game-card__cta {
    color: var(--accent-pink);
  }

  /* ===== PREMIUM ===== */
  .premium {
    position: relative;
    overflow: hidden;
  }

  .premium__bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .premium__card {
    background: var(--gradient-card);
    border: var(--border-glow);
    border-radius: var(--radius-xl);
    padding: clamp(2rem, 5vw, 4rem);
    text-align: center;
    position: relative;
    z-index: 1;
    overflow: hidden;
  }

  .premium__card::before {
    content: '';
    position: absolute;
    inset: -1px;
    border-radius: inherit;
    padding: 1px;
    background: var(--gradient-hero);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0.3;
    pointer-events: none;
  }

  .premium__content h2 {
    margin-bottom: 0.75rem;
  }

  .premium__content > p {
    margin-bottom: 2rem;
  }

  .premium__features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 0.75rem;
    margin-bottom: 2.5rem;
    text-align: left;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .premium__feature {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    font-size: 0.95rem;
    color: var(--text-secondary);
  }

  .premium__check {
    color: var(--accent-green);
    font-weight: 700;
    font-size: 0.85rem;
  }

  .premium__price {
    margin-bottom: 1.5rem;
  }

  .premium__amount {
    font-size: 3rem;
    font-weight: 900;
    background: var(--gradient-hero);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .premium__label {
    display: block;
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-top: 0.25rem;
  }

  /* ===== FINAL CTA ===== */
  .cta {
    padding-bottom: 2rem;
  }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 900px) {
    .hero__inner {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 3rem;
    }

    .hero__subtitle {
      margin-left: auto;
      margin-right: auto;
    }

    .hero__actions {
      justify-content: center;
    }

    .hero__badge {
      margin-left: auto;
      margin-right: auto;
    }

    .hero__screenshot {
      max-width: 280px;
    }

    .showcase__row,
    .showcase__row--reverse {
      grid-template-columns: 1fr;
      gap: 2rem;
      text-align: center;
      direction: ltr;
    }

    .showcase__image img {
      max-width: 260px;
    }

    .feature-list {
      align-items: center;
    }

    .reveal-styles-grid {
      justify-content: center;
    }

    .games__grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .steps {
      flex-direction: column;
      align-items: center;
      gap: 2rem;
    }

    .step__connector {
      display: none;
    }

    .hero__screenshot {
      max-width: 240px;
    }
  }
</style>
